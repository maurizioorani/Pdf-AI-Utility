# This file ensures that the 'test' profile is active when running tests.
# It overrides src/main/resources/application.properties during the test phase.

spring.profiles.active=test

# Optionally, other common test properties that are not profile-specific can go here,
# but it's generally cleaner to keep them in application-test.properties.